@* @using Shop.ModelArea.Models.VModel
@model HomePostVM

<div class="container">
    <div class=" row">
        <div class="col-md-12">
            <form method="post">
                <div class="row align-item-center justify-content-center">
                    <div class="col-md-2">
                        <div class="mb-1">
                            <label class="form-label">Brand Name</label>
                        </div>
                    </div>
                    <div class="col-md-2">
                        <div class="mb-1">
                            <label class="form-label">Brand</label>
                        </div>
                    </div>
                    <div class="col-md-2">
                        <div class="mb-1">
                            <label class="form-label">Brand Type</label>
                        </div>
                    </div>
                    <div class="col-md-2">
                        
                    </div>
                    <div class="col-md-2">
                        
                    </div>
                    <div class="row align-item-center justify-content-center">
                        <div class="col-md-2">
                            <div class="mb-3">
                                <input asp-for="@Model.SearchBox" class="form-control" />
                            </div>
                        </div>
                        <div class="col-md-2">
                            <div class="mb-3">
                                <select asp-for="@Model.BrandId" asp-items="@Model.BrandList" class="form-select" >
                                    <option value="@Guid.Empty">All</option>
                                </select>
                            </div>
                        </div>
                        <div class="col-md-2">
                            <div class="mb-3">
                                <select asp-for="@Model.BrandTypeId" asp-items="@Model.BrandTypeList" class="form-select">
                                    <option value="@Guid.Empty">All</option>
                                </select>
                            </div>
                        </div>
                        <div class="col-md-2">
                            <div class="mb-3">
                                <button class="btn btn-light" type="submit">Search</button>
                            </div>
                        </div>
                        <div class="col-md-2">
                            <div class="mb-3">
                                <a herf="@Url.Action("Index", new {Page = 1 , resetFilter = true})" class="btn btn-primary form-control">Reset</a>
                            </div>
                        </div>
                    </div>
                </div>
            </form>

        </div>

    </div>

</div>

@if (Model.BrandPosts.Count > 0)
{
    <div class="container p-4">
        <div class="row">
            @foreach (var item in Model.BrandPosts)
            {
                <div class="col-md-4">
                    <div class="card border-2 border-dark mb-2 bg-white rounded">
                        <div class="card-header">
                            <img src="@item.Brand.BrandLogo" width="70"/>
                        </div>
                        <div class="card-body">
                            <div class="row">
                                <div class="col-md-6">
                                    <h6 class="text-primary text-uppercase">@item.Brand.BrandName</h6>
                                    <a class="btn btn-outline-light" asp-action="Details" asp-route-id="@item.Id">Details</a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

            }
        </div>
    </div>

}
else
{
    <div class="container">
        <div class=" row">
            <div class="col-md-12">
                <h2>No record</h2>
            </div>
        </div>
    </div>

}

 *@